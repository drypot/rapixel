if [ $# -lt 2 ]; then
  echo 'run [rapixel|osoky|drypot|...] [dev|live]'
  exit 1
fi

if [ '$2' = 'live' ]; then
  NODE_ENV=production
else
  NODE_ENV=development
fi

node modules/main/main.js --config config/$1-$2.json