for arg in ${@-modules}; do
  if [ -f $arg ]; then
    echo mocha $arg
    mocha $arg
  else
    for path in $(find ${arg} -name "*-test.js"); do
      echo mocha $path
      mocha $path
      if [ $? -ne 0 ]; then break; fi
    done
  fi
done
